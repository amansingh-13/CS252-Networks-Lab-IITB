HOW TO RUN?
-----------
1. Run the master script: 

sudo ./master.sh

2. Generate the plots:

sudo ./plotfigs.sh


WHAT ARE THE CONTENTS?
----------------------
1. client.c and serevr.c for demonstrating the TCP variants.
2. singlerun.sh for simulating a single run with given parameters
3. master.sh for simulating all the 18*20 runs and tabulating the values in an output.csv
4. plotfigs.sh for executing plot.py and generating the plots
5. mean.py and stddev.py for generating mean and stddev of numbers passed as arguments
6. plot.py uses matplotlib and pandas to process the output.csv and plat all the 6 figures.